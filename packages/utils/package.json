{
  "name": "@calatrava/utils",
  "version": "0.0.2",
  "description": "A set of tools for working with transactional emails for the Calatrava framework",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "@sindresorhus/tsconfig": "^2.0.0",
    "typescript": "^4.5.3"
  }
}
